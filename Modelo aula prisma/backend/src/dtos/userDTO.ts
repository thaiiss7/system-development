export interface registerUserDto {
    name: string
    password: string
    email: string
    birth_date: Date
}

export interface updateUserDto {
    name: string
    password: string
    email: string
    birth_date: Date
}